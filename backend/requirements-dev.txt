# Development-only dependencies — do NOT install in production
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27  # already in requirements.txt, listed here for test client clarity
fakeredis>=2.20  # in-process Redis for session-store / rate-limit tests (no server needed)
